import { ConstructionSection } from '@/components/ConstructionSection';

/**
 * Purchasing page (Under Construction).
 */
export default function NakupPage() {
    return (
        <div className="flex-1 overflow-auto">
            <ConstructionSection
                title="Nákup"
                description="Připravujeme modul pro správu nákupních objednávek, sledování dodávek a automatizaci skladových zásob."
            />
        </div>
    );
}
